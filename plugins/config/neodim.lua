require("neodim").setup {
  alpha = 0,
  blend_color = "#464646",
  update_in_insert = {
    enable = true,
    delay = 100,
  },
  hide = {
    virtual_text = true,
    signs = true,
    underline = true,
  },
}
